apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/rfvermut/python-http2:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/rfvermut/python-http2:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: python-http2
        chartPath: charts/python-http2
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/rfvermut/python-http2"
          image.tag: "{{.DIGEST_HEX}}"
